import { createRoot } from "react-dom/client";

//variable = component Defintion : variable pattern
const Heading = <h1>Hello React!</h1>

createRoot(document.getElementById('root')).render(Heading)

